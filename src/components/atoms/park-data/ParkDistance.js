import createTextComponent from './GenericText'

const parkDistance = {
    fontSize: "1.6em",
    fontWeight: "bold"
}
const ParkDistance = createTextComponent(parkDistance);

export default ParkDistance;
